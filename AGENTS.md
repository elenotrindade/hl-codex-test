# Agent Instructions

- Create pull requests against `master` for now.
- Before opening a PR, make sure the branch is up to date with `origin/master` and has no unresolved merge conflicts.
- Run the existing verification command for the change when applicable.
